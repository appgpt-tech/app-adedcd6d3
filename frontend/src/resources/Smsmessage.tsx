//Source code generated by AppGPT (www.appgpt.tech)
import {
  Datagrid,
  List,
  EditButton,
  Edit,
  SimpleForm,
  Create,
  SelectColumnsButton,
  DatagridConfigurable,
  TopToolbar,
  CreateButton,
  ExportButton,
  FilterButton,
  //Field controls
  BooleanField,
  DateField,
  EmailField,
  ImageField,
  NumberField,
  ReferenceField,
  TextField,
  UrlField,
  //Input controls
  BooleanInput,
  DateInput,
  EmailInput,
  ImageInput,
  NumberInput,
  ReferenceInput,
  TextInput,
  UrlInput,
  PasswordInput,
} from 'react-admin';
import { useRecordContext } from 'react-admin';
import { Grid } from '@mui/material';
const ReadOnlyPasswordField = ({ record, source }) => {
  // You can customize the way you display the password here, e.g., mask it with asterisks
  const maskedPassword = '********';

  return <span>{maskedPassword}</span>;
};
const ListActions = () => (
  <TopToolbar>
    <FilterButton />
    <CreateButton />
    <ExportButton />
    <SelectColumnsButton />
  </TopToolbar>
);
const SmsmessageTitle = () => {
  const record = useRecordContext();
  return <span>Smsmessage {record ? `"${record.id}"` : ''}</span>;
};

export const SmsmessageList = () => (
  <List actions={<ListActions />} filters={ResourceFilters}>
    <DatagridConfigurable>
      <TextField source="sender" />
      <TextField source="messageBody" />
      <DateField source="receivedAt" />
      <BooleanField source="processed" />
      <EditButton />
    </DatagridConfigurable>
  </List>
);

export const SmsmessageEdit = () => (
  <Edit title={<SmsmessageTitle />}>
    <SimpleForm>
      <Grid container rowSpacing={1} columnSpacing={{ xs: 1, sm: 2, md: 5 }}>
        <Grid item xs={4}>
          <TextInput source="sender" />
        </Grid>
        <Grid item xs={4}>
          <TextInput source="messageBody" />
        </Grid>
        <Grid item xs={4}>
          <DateInput source="receivedAt" />
        </Grid>
        <Grid item xs={4}>
          <BooleanInput source="processed" />
        </Grid>
      </Grid>
    </SimpleForm>
  </Edit>
);

export const SmsmessageCreate = () => (
  <Create>
    <SimpleForm>
      <Grid container rowSpacing={1} columnSpacing={{ xs: 1, sm: 2, md: 5 }}>
        <Grid item xs={4}>
          <TextInput source="sender" />
        </Grid>
        <Grid item xs={4}>
          <TextInput source="messageBody" />
        </Grid>
        <Grid item xs={4}>
          <DateInput source="receivedAt" />
        </Grid>
        <Grid item xs={4}>
          <BooleanInput source="processed" />
        </Grid>
      </Grid>
    </SimpleForm>
  </Create>
);

const ResourceFilters = [
  <TextInput source="q" label="Search" alwaysOn />,
  ,
  ,
  ,
  ,
];
